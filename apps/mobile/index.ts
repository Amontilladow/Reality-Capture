import { registerRootComponent } from 'expo';
import App from './App';

// registerRootComponent calls AppRegistry.registerComponent('main', () => App),
// and sets up the environment appropriately whether running in Expo Go, a
// development build, or a standalone production build.
registerRootComponent(App);
