# Vite proxies /api -> http://localhost:3000 in dev (see vite.config.ts).
# Nothing to configure for local development. For a standalone build against
# a different API host, set VITE_API_BASE and read it in src/lib/api.ts.
VITE_API_BASE=/api/v1
